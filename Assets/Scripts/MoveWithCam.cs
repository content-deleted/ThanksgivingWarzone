using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MoveWithCam : MonoBehaviour
{
    //void FixedUpdate() {if(CameraController.singleton.moving) transform.position += new Vector3(0,CameraController.singleton.speed,0);}
}
